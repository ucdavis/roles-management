{
  id: "Clusters",
  name: "Clusters",
  data: {},
  children: [
  <% @roots.each do |root| %>
  {
    id: "<%= root.id %>",
    name: "<%= root.name %>",
    data: {},
    children: [
      <% root.children.each do |department| %>
        {
          id: "<%= department.id %>",
          name: "<%= department.name %>",
          data: {},
          children: []
        }<% unless department == root.children.last %>,<% end %>
      <% end %>
    ]
  }<% unless root == @roots.last %>,<% end %>
  <% end %>
  ]
}